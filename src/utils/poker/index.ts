// eslint-disable-next-line prefer-destructuring, @typescript-eslint/no-unused-expressions
require('./pokerResolver').Hand;

// eslint-disable-next-line prefer-destructuring
const Hand = window.Hand;

export {
  Hand,
};
